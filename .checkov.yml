skip-check:
  # Networking - architectural decisions
  - CKV_AWS_130  # Public subnets require map_public_ip_on_launch for ALB and frontend instances in public tier
  - CKV_AWS_88   # Frontend instances in public subnet need public IPs for internet-routed traffic via IGW
  - CKV_AWS_260  # Internet-facing ALB must accept HTTP on port 80 from anywhere by design

  # HTTPS - requires domain + ACM certificate (TODO: unskip when cert is available)
  - CKV_AWS_103  # TLS 1.2 requires HTTPS listener - pending ACM certificate
  - CKV_AWS_378  # ALB HTTP protocol - pending HTTPS listener setup
  - CKV2_AWS_20  # HTTP to HTTPS redirect - pending HTTPS listener setup

  # Multi-AZ - cost consideration (TODO: unskip for prod)
  - CKV_AWS_157  # Multi-AZ RDS deferred - cost consideration for dev environment

  # ALB access logs - requires dedicated S3 bucket (TODO: unskip when bucket is created)
  - CKV_AWS_91   # ALB access logs require dedicated S3 bucket - planned as follow-up

  # Bootstrap S3 bucket - separate one-time module, not part of main deployment
  - CKV2_AWS_61  # S3 lifecycle config - state bucket rarely changes
  - CKV_AWS_144  # S3 cross-region replication - single-region project
  - CKV_AWS_18   # S3 access logging - would need another bucket for logs
  - CKV2_AWS_62  # S3 event notifications - not needed for state bucket
